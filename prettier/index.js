const createConfig = () => {
  return {
    singleQuote: true,
    trailingComma: 'all',
    tabWidth: 2,
    semi: true,
    bracketSpacing: true,
    arrowParens: 'always',
    printWidth: 120,
  };
};

module.exports = {
  createConfig,
};
